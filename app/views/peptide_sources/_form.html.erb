<%= form_for(@peptide_source) do |f| %>
  <% if @peptide_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peptide_source.errors.count, "error") %> prohibited this peptide source from being saved:</h2>

      <ul>
      <% @peptide_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
